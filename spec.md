# Specification

## Summary
**Goal:** Rename the application to "TickTockTask".

**Planned changes:**
- Update the `APP_NAME` constant in `frontend/src/branding.ts` to `'TickTockTask'`

**User-visible outcome:** The app displays "TickTockTask" everywhere the app name appears, including the browser tab title and all UI locations referencing the app name.
